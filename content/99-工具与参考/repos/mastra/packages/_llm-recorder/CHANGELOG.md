---
title: @internal/llm-recorder
author: mastra-ai
date: 2026-05-28
source: https://github.com/mastra-ai/mastra/blob/main/packages/_llm-recorder/CHANGELOG.md
tags:
  - agent-framework
  - typescript
  - mastra
---

# @internal/llm-recorder

## 0.0.35

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.34

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.33

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.32

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.31

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.30

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.29

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.28

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.27

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.26

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.25

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.24

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.23

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.22

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.21

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.20

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.19

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.18

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.17

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.16

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.15

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.14

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.13

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.12

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.11

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.10

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.9

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.8

### Minor Changes

- Added binary artifact support for non-JSON request/response payloads (for example audio) in the LLM recorder. ([#14274](https://github.com/mastra-ai/mastra/pull/14274))

  Binary bytes are now written as hash-based sidecar files in `__recordings__/` and referenced from JSON recordings with metadata (`contentType`, `size`, and artifact `path`). Replay restores the original binary payload and content-type headers from artifacts, while keeping JSON fixtures small and readable.

## 0.0.7

## 0.0.6

## 0.0.5

## 0.0.4

## 0.0.3

## 0.0.2
