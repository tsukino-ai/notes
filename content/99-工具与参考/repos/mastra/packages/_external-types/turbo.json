{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build:lib": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "!dist/docs/**"],
      "inputs": ["$TURBO_DEFAULT$", "./tsup.config.ts"]
    },
    "build": {
      "dependsOn": ["build:lib"],
      "inputs": ["package.json"],
      "outputs": ["dist/**", "!dist/docs/**"]
    }
  }
}
