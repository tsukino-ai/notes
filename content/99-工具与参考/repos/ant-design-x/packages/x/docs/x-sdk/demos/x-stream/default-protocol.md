---
title: default-protocol
author: ant-design
date: 2026-05-28
source: https://github.com/ant-design/x/blob/main/packages/x/docs/x-sdk/demos/x-stream/default-protocol.md
tags:
  - react
  - ai-ui
  - ant-design-x
  - component-library
---

## zh-CN

> SSE - https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events

XStream 默认�?`transformStream` 是用�?SSE 协议的流转换器。`readableStream` 接收一�?`new ReadableStream(...)` 实例，常见的�?`await fetch(...).body`

## en-US

> SSE - https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events

The default `transformStream` in XStream is an SSE protocol stream transformer and is optional. The `readableStream` accepts a `new ReadableStream(...)` instance, such as `await fetch(...).body`
