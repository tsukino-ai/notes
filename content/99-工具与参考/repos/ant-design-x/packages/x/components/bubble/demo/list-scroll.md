---
title: list-scroll
author: ant-design
date: 2026-05-28
source: https://github.com/ant-design/x/blob/main/packages/x/components/bubble/demo/list-scroll.md
tags:
  - react
  - ai-ui
  - ant-design-x
  - component-library
---

## zh-CN

可以使用 `ref` 控制列表滚动条。当 **Bubble.List** 内容在不断增长且通过 `ref.scrollTo` 跳转到底部时，`behavior: 'smooth'` 的行为会�?`behavior: 'instant'` 替代�?

## en-US

Bubble.List ref. `behavior: 'smooth'` would be replaced by `behavior: 'instant'` when the content of **Bubble.List** growing constantly and you jump to the bottom using `ref.scrollTo`.
